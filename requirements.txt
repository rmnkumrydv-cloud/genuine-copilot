# Deterministic core (Gates 0-3). Fast, low-risk install — no ML wheels.
# RAG/LLM deps live in requirements-ml.txt so this stays quick to install.

# Data model & config
pydantic>=2.6,<3
pydantic-settings>=2.2
pyyaml>=6.0
python-dotenv>=1.0

# Ingestion
gitpython>=3.1.43
requests>=2.32

# Scaling / fingerprinting
datasketch>=1.6
numpy>=1.26

# Optional benchmark clone matcher (swappable behind CloneMatcher interface).
# Core similarity uses stdlib ast+tokenize+difflib and works without this.
copydetect>=0.5.0

# API server
fastapi>=0.110
uvicorn[standard]>=0.29

# Testing
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
