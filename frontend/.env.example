# Base URL for the Genuine API. Leave unset in dev — Vite proxies /api -> :8000.
# In production (e.g. Vercel), point this at your deployed backend origin.
# VITE_API_BASE=https://your-genuine-api.example.com
